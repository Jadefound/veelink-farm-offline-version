// Import the polyfill first to ensure it's available before any other imports
import 'react-native-get-random-values';

// Then import the main entry point
// For Expo Router projects, we use the expo-router/entry as the main entry point
import 'expo-router/entry';